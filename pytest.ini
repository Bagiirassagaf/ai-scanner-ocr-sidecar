[pytest]
pythonpath = .
asyncio_default_fixture_loop_scope = function
markers =
    integration: spawns real OS subprocesses/is slower than the rest of the unit suite (not an external-service dependency)
